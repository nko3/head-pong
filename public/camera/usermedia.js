// Generated by CoffeeScript 1.4.0
(function() {
  var handle_device, not_available, not_supported, shimGetUserMedia;

  shimGetUserMedia = function() {
    return navigator.getUserMedia || navigator.webkitGetUserMedia || navigator.mozGetUserMedia || navigator.msGetUserMedia;
  };

  not_supported = function() {
    return $("#message").html("We're using cool stuff, so you need to move to a decent browser, like chrome.");
  };

  if (shimGetUserMedia) {
    shimGetUserMedia({
      video: true,
      audio: true
    }, function(media_stream) {
      return alert("got it");
    }, not_available);
  }

  not_available = function() {
    return $("#message").html("Not able to connect with your camera device, did you authorized us?");
  };

  handle_device = stream(function() {
    return alert("just got the stream!");
  });

}).call(this);
